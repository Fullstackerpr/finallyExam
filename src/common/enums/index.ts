export enum Roles {
  SUPERADMIN = 'superadmin',
  ADMIN = 'admin',
  PATIENT = 'patient',
  DOCTOR = 'doctor',
}

export enum Status {
  ACTIVE = 'active',
  INACTIVE = 'inactive',
}

export enum StatusMedic {
  PROGRESS = 'progres',
  COMPLETED = 'completed',
  REJECTED = 'rejected',
  CANCEL = 'cancel',
}

export enum State {
  PENDING = 'pending',
  CONFIRM = 'confirm',
  CANCEL = 'cancel',
  COMPLETED = 'completed',
}

export enum Gender {
  MALE = 'male',
  FEMALE = 'female',
}
